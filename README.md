# OrthoNow Martech Implementation: GTM, LP & CRM Integration

This repository contains the first-month growth marketing and marketing technology deliverables for OrthoNow, a network of 9 orthopaedic clinics in Bengaluru, Hyderabad, and Chennai. The project tackles a critical conversion gap (current 2.1% landing page conversion vs. 6-8% industry benchmark) by implementing a high-converting, mobile-first consultation landing page, building a production-ready Google Tag Manager (GTM) event schema, and designing an enterprise-grade HubSpot CRM and Karix WhatsApp notification architecture.


```

---

##  Task 01 Summary: GTM Event Schema Key Decisions

* **Validation-First Triggers:** All step-level form tracking events use Custom Event triggers rather than generic click triggers. This ensures data cleanliness by only firing event pushes *after* frontend regex validation succeeds.
* **API Success Binding:** The final conversion event (`booking_confirmed` at Step 3) is bound strictly to the server-side API HTTP 200 success callback, preventing phantom conversions in GA4 from database write failures.
* **Single Scroll Event Strategy:** Rather than cluttering the namespace with multiple events for scroll depths, we mapped all scroll milestones (25%, 50%, 75%, 90%) to a single custom event (`blog_scroll_depth`) passing the dynamic percentage as a parameter.
* **Privacy-First Data Hygiene:** Personally Identifiable Information (PII) is kept out of GA4 parameters to prevent account suspension, using client-side hashing to associate actions anonymously.

---

##  Task 02: Landing Page Performance & Testing

The landing page (`index.html`) is built from scratch without external dependencies, web frameworks, or CDN libraries, optimizing it for lightning-fast loads.

### How to Open & Verify

#### SCENARIO A — View Locally:
1. Download or clone this repository.
2. Open `index.html` directly in Google Chrome (double-click the file OR drag it into a Chrome window).
3. No server required — the page is fully self-contained.

#### SCENARIO B — View Live (Netlify Deploy — free, 2 minutes):
1. Go to netlify.com and sign up free.
2. Drag and drop the `index.html` file onto the Netlify dashboard deploy area.
3. Netlify generates a live URL instantly (e.g. `https://random-name.netlify.app`).
4. Use this URL to run PageSpeed Insights: pagespeed.web.dev → paste URL → Mobile tab.

### How to Test dataLayer Pushes (Console Verification)
To see dataLayer events in action:
* Open `index.html` in Chrome (locally or via Netlify URL).
* Open Chrome Developer Tools by pressing `F12` or right-clicking and selecting **Inspect**.
* Toggle the **Device Toolbar** (`Ctrl + Shift + M` / `Cmd + Shift + M`) and select a mobile view (e.g., iPhone 12/14 Pro).
* Verify that the **Hero Headline**, **Subheadline**, **Urgency Slot Widget**, **Form**, and **CTA Button** are all fully visible above the fold on mobile without scrolling.
* In the Developer Tools, select the **Console** tab.
* Fill out the form with a name (e.g., `Amit Sharma`) and a valid Indian phone number (e.g., `9845012345`).
* Click the **Check Specialist Availability** CTA.
* The form container will transition immediately to the **Thank You State** (displaying booking confirmation details and a WhatsApp CTA).
* Run the following command in the browser console:
  ```javascript
  console.table(window.dataLayer);
  ```
* You will see the `consultation_form_submitted` event push along with its custom variables:
  ```json
  {
    "event": "consultation_form_submitted",
    "form_name": "ortho_consultation_lp",
    "clinic_preference": "[from ?clinic= URL param or fallback]",
    "lead_source": "google_ads_consultation_lp",
    "page_url": "https://random-name.netlify.app",
    "timestamp": "2026-07-01T12:50:00.000Z",
    "patient_name_hashed": "[real SHA-256 hex string]",
    "patient_phone_hashed": "[real SHA-256 hex string]"
  }
  ```
*Note: `patient_name_hashed` and `patient_phone_hashed` are real SHA-256 hashes generated by the Web Crypto API. The actual hex output will vary by input value.*
* Test clicking the sticky header **Call Now** button or the floating **WhatsApp Widget** to verify that the `call_now_click` and `whatsapp_click` events are pushed to the console array.

---

## ⚙️ Task 03 Summary: Integration Architecture Decision

* **Node.js Lambda Middleware:** We designed a custom API Middleware deployed on AWS Lambda instead of native embeds or Zapier to run the necessary pre-delivery checks.
* **Search-Before-Create Deduplication:** To address HubSpot’s default email-centric deduplication, the middleware executes a search against the `phone` property (`POST /crm/v3/objects/contacts/search`) before inserting leads. Existing numbers trigger a `PATCH` update, while new ones trigger a `POST` create, completely eliminating duplicate contact cards.
* **AWS SQS Dead-Letter Queue (DLQ):** To safeguard the 2-minute SLA for WhatsApp delivery, a message queue handles failures with exponential retry logic. Slack/SMS webhooks instantly alert the engineering team if a webhook drops.

---

##  PageSpeed Insights Score

The landing page achieved a mobile-first performance score of **98** using the following optimization strategies:
1. **Critical CSS Inlining:** All CSS rules are embedded inside a `<style>` tag, avoiding external round-trips.
2. **Font Preconnecting:** Preconnect headers are used for fonts.googleapis.com and fonts.gstatic.com to speed up DNS lookup, using `display=swap` to avoid render blocking.
3. **SVG & CSS-Only Graphics:** All icons, star ratings, avatar indicators, and backgrounds are styled using pure CSS shapes and SVG vector data, removing image asset download lag entirely.
4. **Minified Script Payload:** Asynchronous code execution and short helper loops keep script parse times minimal.

![PageSpeed Insights Mobile Score](/screenshots/pagespeed_mobile.png)

Score of 98 achieved on Mobile PageSpeed Insights. To verify: deploy `index.html` to Netlify (free) and run the live URL through pagespeed.web.dev. See `/screenshots/pagespeed_mobile.png` for the result.
